<h4><%= content_tag :span, raw(@next) + ': ', class: 'event-date'%> <%= @event.title %></h4>
<p><%=raw @repetition_string %></p>


<div class="content">
  <%= raw @event.prose %>
</div>

<% if logged_in? %>
  <p class='war-room'><%= link_to "Edit or Destroy", edit_event_path(@event) %></p>
<% end %>
